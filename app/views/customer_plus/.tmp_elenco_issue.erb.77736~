 <h3><%= ("Segnalazioni dell'utente") %></h3>
 <%
  @customer= Customer.find_by_id(params[:customer])
@issue_to_show = []
      @issue = Issue.all(:conditions => {:project_id => @customer.projects})
		@issue.each do |issue_to_control|
		
       		issue_to_control.custom_field_values.each do |value|
            	  if (Setting["plugin_redmine_customer_plus"]['save_customer_to'].include?(value.custom_field.id.to_s)) && (@customer.name == value.value)
					%>
                	 <p>
    					<%=link_to issue_to_control.subject, :controller => 'issues', :action => 'show', :id => issue_to_control.id  %>
  					 </p>
				  <%
				  @issue_to_show.push(issue_to_control.id)
              	end
         	 end
		end
%>

<br>
<br>
<br>
<br>
<br>
<br>


 <%= link_to "indietro", {:controller => 'customer_plus', :action => 'show', :customer_id => @customer.id, :project_id => @project} %>