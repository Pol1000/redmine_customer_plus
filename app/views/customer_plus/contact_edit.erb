<div class="contextual">
  <%= link_to_if_authorized l(:label_customer_list), {:controller => 'customers', :action => 'list', :id => @project}, :class => 'icon icon-users' %>
  <%= link_to_if_authorized l(:label_customer_new), {:controller => 'customers', :action => 'new', :id => @project}, :class => 'icon icon-add' %>
</div>
<h2><%= link_to l(:label_customer_plural), 'list' %> &#187; <%= 
  link_to @customer.pretty_name, :controller => 'customer_plus', :action => :edit, :customer_id => @customer.id
%>  &#187;  <%= h @customer_contact.pretty_name %></h2>

<% labelled_tabular_form_for :customer_contact, @customer_contact, :url =>{:action => 'contact_update', :customer => @customer} do |f|  %>
  <%= render :partial => 'contact_form', :locals => {:f => f}  %>
  <%= submit_tag l(:button_save) -%>
<% end -%>


<% content_for :header_tags do %>
    <%= stylesheet_link_tag 'customer_plus', :plugin => 'redmine_customer_plus' %>
<% end %>
